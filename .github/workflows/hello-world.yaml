name: hello-world


on:
    push:
        branches:
        - main

jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: Dire Bonjour
              run: 
                echo "Bonjour, le monde!"
                echo "Workflow execute ave success"
                date
                